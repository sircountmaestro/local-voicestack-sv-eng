# Azure Speech-gateway (svensk TTS)

Lyssnar på `127.0.0.1:5050`. Kräver egen nyckel i `.env` (skapas tom av install). Startas av `tts` bara om nyckeln är ifylld.

Se rotens [README.md](../../README.md) avsnitt **Azure-nyckel**.
