services:
  azure-speech-gateway:
    build: .
    image: local-voicestack-azure:1.0.0
    container_name: azure-speech-gateway
    restart: unless-stopped
    env_file:
      - .env
    environment:
      LISTEN_HOST: "0.0.0.0"
      LISTEN_PORT: "5050"
    ports:
      - "127.0.0.1:5050:5050"
